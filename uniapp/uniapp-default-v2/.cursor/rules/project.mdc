# 项目通用规范

## 命名

1. 文件名、文件夹名、变量名、函数名等应该使用小驼峰命名法或者中划线命名法，例如：`userInfo`、`user-info`。
2. 组件名应该以大写字母开头，例如：`MyComponent`。
3. 常量名应该全部大写，单词之间使用下划线分隔，例如：`MAX_VALUE`。

## 结构

1. 项目应该有清晰的目录结构，通常包括 `pages`、`components`、`static`、`utils` 等目录。
2. 每个页面应该有自己的目录，包括 `.vue` 文件、相关的样式文件（如 `.css`、`.scss`）、图片等资源。
3. 公共组件应该放在 `components` 目录下，可以根据功能进行分类。
4. 工具函数应该放在 `utils` 目录下，可以根据功能进行分类。

## 代码风格

1. 代码应该遵循 ES6 标准，尽量使用箭头函数、模板字符串、解构赋值等新特性。
2. 每个文件应该只包含一个 Vue 组件，如果一个文件包含多个组件，应该将它们分离到不同的文件中。
3. 组件的 props 应该使用驼峰命名法，并且应该使用类型检查和默认值。
4. 组件的事件应该使用 kebab-case 命名法，例如：`@my-event`。
5. 组件的样式应该使用 scoped 属性，以避免全局污染。
6. 应该使用 ESLint 进行代码检查，并且应该遵循 Airbnb 的 JavaScript 风格指南。

description:
globs:
alwaysApply: false

---
